import 'package:flutter/material.dart';

/// Configuración de colores para la aplicación
/// Contiene todos los colores utilizados en la app para mantener consistencia visual
class AppColors {
  // Colores primarios
  static const Color primaryBlue = Color(0xFF2196F3);
  static const Color primaryGreen = Color(0xFF4CAF50);
  
  // Colores para contenedores
  static const Color userInfoContainer = Color(0xFF1976D2); // Azul oscuro
  static const Color tasksContainer = Color(0xFF388E3C); // Verde oscuro
  static const Color compareContainer = Color(0xFF37474F); // Gris azulado
  static const Color transitContainer = Color(0xFF5D4037); // Marrón
  
  // Colores de texto
  static const Color textWhite = Colors.white;
  static const Color textDark = Color(0xFF263238);
  
  // Colores de fondo
  static const Color backgroundColor = Color(0xFFF5F5F5);
}
